<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.misaac.test1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/monica_launcher"
        android:label="Dose Calculater"
        android:roundIcon="@drawable/ic_menu_camera"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DiseaseID2"
            android:label="@string/title_activity_disease_id2"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>